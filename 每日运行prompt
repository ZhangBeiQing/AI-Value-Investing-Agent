首先激活虚拟环境：source /home/zhangbeiqing/venv/ai_stock/bin/activate


要求：搜索时请优先使用阿里云百炼的search mcp


1、python scripts/refresh_all_for_date.py --date cur_date
这里cur_date代表当前的日期，这个运行可能需要15分钟左右，请设好等待时间不要超时



2、步骤1完成后并发的起3个subagent，分别完成下面任务：
1）起一个subagent执行以下指令 "开始 cur_date 的宏观总结"，这将触发skill daily-macro-summary
2) 并发再起一个subagent执行以下命令"开始 cur_date 的渐进式新闻总结，没用的已经过时的新闻就删掉，不要让渐进式新闻总结文件太大", 这将触发skill gradual-hot-news-summary。注意渐进式新闻总结需要的时间较长，大概20分钟左右
3) 并发再起一个subagent进行财报总结，这会触发skill financial-report-summary。在此之前我们需要先运行scripts/start_mineru_api.sh启动mineru程序(看下mineru有没有已经启动了，如果已经启动了就不需要运行这个脚本了)，用于把财报pdf转换为agent更容易分析的markdown格式。然后请按照financial-report-summary skill先在wsl运行命令 python scripts/prepare_financial_report_skill.py --date cur_date --sync-first --json --include-quant-prefilter
这个命令可能会运行10-30分钟，主要是如果有公司新发了财报需要把pdf转成markdown需要一些时间，请合理设置等待时间，避免超时中断命令。然后按照skill要求把需要更新财报的公司的财报进行更新

3、最后请运行"python scripts/run_daily_pipeline.py --date cur_date --max-workers 6 --all-books" 命令更新所有股票研究包信息


一次性或者说在一个会话里运行完上面的所有命令，不要因为等待而中断